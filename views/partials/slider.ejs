<section class="chat-slider-section align-middle">
  <div class="slider-text text-center">
    <h2 class="text-center">Remove Your Loneliness</h2>
    <p>Connect with people randomly and anonymously from all over the world — start a meaningful conversation today!</p>
  </div>

  <div class="carousel-wrapper">
    <div class="carousel-track">
      <div class="carousel-slide">
        <img src="/images/4433.jpg" alt="Chat Preview 1">
      </div>
      <div class="carousel-slide">
        <img src="/images/woman-having-video-call-home-laptop-device.jpg" alt="Main Chat">
      </div>
      <div class="carousel-slide">
        <img src="/images/Screenshot_6-7-2025_222445_www.freepik.com.jpeg" alt="Chat Preview 2">
      </div>
      <div class="carousel-slide">
        <img src="https://th.bing.com/th/id/OIP.yJF5wBMqtdKiJ0NsyW9SlAHaEK?w=326&h=183&c=7&r=0&o=7&pid=1.7&rm=3" alt="Chat Preview 1 (Duplicate)">
      </div>
    </div>
  </div>
  <style>
    .carousel-wrapper {
  overflow: hidden;
  width: 90%;
  max-width: 800px;
  margin: 0 auto;
  border-radius: 12px;
  box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
}
.carousel-slide>img{
    height: 50%;
    width: 100%;
}
.carousel-track {
  display: flex;
  transition: transform 0.5s ease-in-out;
}

.carousel-slide {
  min-width: 100%;
  flex-shrink: 0;
}

.carousel-slide img {
  width: 100%;
  height: auto;
  display: block;
  object-fit: cover;
  border-radius: 12px;
}

/* Optional dark mode styling */
.dark-mode .carousel-wrapper {
  box-shadow: 0 6px 15px rgba(255, 255, 255, 0.1);
}

  </style>
 <script>
  const track = document.querySelector('.carousel-track');
  const slides = document.querySelectorAll('.carousel-slide');
  let currentSlide = 0;

  function moveToNextSlide() {
    currentSlide++;
    if (currentSlide >= slides.length - 1) {
      track.style.transition = 'none';
      track.style.transform = 'translateX(0)';
      currentSlide = 0;
      setTimeout(() => {
        track.style.transition = 'transform 0.5s ease-in-out';
        moveToNextSlide();
      }, 50);
      return;
    }
    track.style.transform = `translateX(-${currentSlide * 100}%)`;
  }

  setInterval(moveToNextSlide, 4000); // Change slide every 4 seconds
</script>

</section>
